import React from 'react';
import { linkTo } from '@storybook/addon-links';
import App from '../App';

export default {
  title: 'Product Components App',
  component: App,
};

export const Start = () => <App />;

